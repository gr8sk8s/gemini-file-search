### Once you have set the Env vars below to your satisfaction,
### then ensure that the file is saved as ".env" ; it will be loaded at app runtime

# Google Gemini API Configuration
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_api_key_here

# Optional: Gemini model to use for generation; choose a valid supported model
# Defaults to: gemini-2.5-flash
USE_MODEL=gemini-2.5-flash
# USE_MODEL=gemini-2.5-pro

# Optional: Display name for the file search store; can be any name
# Defaults to: demo_filesearch_store
FILE_SEARCH_STORE=demo_filesearch_store

